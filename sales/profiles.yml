# example profiles.yml file
sales:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: user
      password: password
      port: 31770
      dbname: mydbt
      schema: dbt
      threads: 1